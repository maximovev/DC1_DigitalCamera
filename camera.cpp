#include <stdio.h>
#include "camera_v2.h"

using namespace maxssau;

int main(int arg_count, char* arg_values[])
{
	rpi_camera cam;
	//camera cam(formats::SRGGB10,2592,1944);
	//unsigned int *buffer;
	//cam.GetFrame(buffer);
	return 0;
}
